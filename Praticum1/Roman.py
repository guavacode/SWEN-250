import sys

def roman( entered ):
   print(entered)
   # the enciphered string to return 
   enciphered = ''
   
   # traverse the entered string and create an enciphered string
   # substituting characters as per the algorithm.

   return enciphered

def main():
   for line in sys.stdin:
      line = line.rstrip()
   print(roman(line))

main()
